import { COLORS } from "../ui";

export const global = `
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  ul {
    list-style: none;
  }
  a {
    text-decoration: none;
    color: ${COLORS.BLACK_LIGHT};
  }
`;